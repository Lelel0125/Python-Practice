# Typecasting = the process of converting a variable from one data type to another
#               str(), int(), float(), bool()

name = "Bro code"
age = 25
gpa = 3.2
is_Student = True

# gpa = int(gpa)
# age = float(age)
# age = str(age)
name = bool(name) # empty name be False

# age += "1" ("string plus" will be 251)

print(name)